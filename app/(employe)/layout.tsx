/**
 * Layout for the employee-facing routes (/e/*, /caisse).
 * Minimal layout with no authentication sidebar.
 */
export default function EmployeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
