return NextResponse.json({ transaction_id: transaction.id, created_at: transaction.created_at, lignes: ticketLignes, montant_total: montantTotalServeur, remise, mode_paiement, client: updatedClient, }); }